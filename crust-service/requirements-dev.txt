# ============================================================================
# CRUST Verification Service - Dev / test dependencies
# Install: pip install -r requirements-dev.txt
# (also installs requirements.txt automatically via -r below)
# ============================================================================

-r requirements.txt

# -- Testing -------------------------------------------------------------------
pytest==8.2.0
pytest-asyncio==0.23.6             # async test support
pytest-cov==5.0.0                  # coverage reporting
httpx==0.27.0                      # async test client for FastAPI (TestClient)

# -- Linting / formatting ------------------------------------------------------
ruff==0.4.4                        # fast linter + formatter (replaces flake8/black)
mypy==1.10.0                       # static type checking
types-python-dateutil==2.9.0.20240316

# -- Dev server -----------------------------------------------------------------
watchfiles==0.21.0                 # used by uvicorn --reload